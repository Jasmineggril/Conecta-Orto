{
  "buildCommand": "pnpm install && NODE_ENV=production pnpm --filter @workspace/conecta-orto run build && cp -r artifacts/conecta-orto/dist/public ./dist-vercel",
  "outputDirectory": "dist-vercel",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=3600" }]
    }
  ],
  "rewrites": [
    { "source": "/((?!assets|favicon|robots|videos|planta).*)", "destination": "/index.html" }
  ]
}
